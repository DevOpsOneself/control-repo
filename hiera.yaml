---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: 'Yaml backend'
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "roles/%{facts.role}.yaml"
      - "role/%{facts.role}"
      - "modules/%{module_name}"
      - 'common.yaml'
